import type { WeaponType } from "./loaders/queries.js";

/** Spanish display labels for weapon types (Espada Larga / Gran Espada). */
export const WEAPON_LABELS: Record<WeaponType, string> = {
  longsword: "Espada Larga",
  greatsword: "Gran Espada",
};
